#![forbid(unsafe_code)]
//! Identify by dns: reads the peer's reverse name, from the resolver the node is given; a
//! transport-layer identifier whose claim is detected.
//!
//! Declared and not yet written: `architecture.toml` carries the maturity. When it
//! is, it implements `TransportIdentifier` (ADR-0050).
